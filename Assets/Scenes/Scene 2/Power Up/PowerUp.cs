using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Events;


public class PowerUp : MonoBehaviour
{
  //  [SerializeField] float speed = 360f;
    // Start is called before the first frame update

    //public UnityEvent PowerUp1;
   // GameObject[] obj;


   /* void Start()
    {

        obj = GameObject.FindGameObjectsWithTag("gate");
        if (obj != null)
        {
            foreach (GameObject gate in obj)
            {
                PowerUp1.AddListener(gate.GetComponent<GateOpen>().OnPowerUp1);

            }

        }

    }

    // Update is called once per frame

    void Update()
    {
        transform.Rotate(speed * Time.deltaTime * Vector3.forward);
    }




    public void OnPowerUp1()
    {
        Debug.Log("Star Power Up activated!");
        //PowerUp.Invoke();
    }

    private void OnTriggerEnter(Collider other)
    {
        PowerUp1.Invoke();
        Debug.Log("hey girl");
        Destroy(gameObject);

    }*/

}
